import { useState } from 'react'; import { Reveal } from './useReveal.jsx'
const photos=[['building','School Building'],['classroom','Classroom'],['students','Students'],['activity','Activities']]
export default function Gallery(){const [selected,setSelected]=useState('');return <section id="gallery" className="section alt"><div className="container"><div className="section-title"><div className="kicker">Campus life</div><h2>Moments made for learning.</h2><p>Replace these visual placeholders with photos from Zainab Campus.</p></div><Reveal><div className="gallery">{photos.map(x=><div className="photo" key={x[1]} onClick={()=>setSelected(x[1])}><i className={`scene ${x[0]}`}/><span>{x[1]} ↗</span></div>)}</div></Reveal>{selected&&<div className="modal" onClick={()=>setSelected('')}><div><h2>{selected}</h2><p>Campus photo placeholder — click anywhere to close.</p></div></div>}</div></section>}
